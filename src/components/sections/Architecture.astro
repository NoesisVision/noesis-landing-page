---
import Container from "../shared/Container.astro";

export interface Props {
    className?: string;
}

const { className } = Astro.props;
---

<section class="relative" id="architecture">
    <Container className={className}>
        <div class="flex flex-col items-center justify-center py-2 md:py-4">
            <div class="w-full max-w-screen-xl mx-auto overflow-hidden">
                <div class="aspect-[16/9] w-full relative">
                    <img 
                        src="/images/noesis-architecture-2-1-conventions-simple.svg" 
                        alt="System architecture" 
                        class="w-full h-full object-contain"
                        loading="lazy"
                    />
                </div>
            </div>
        </div>
    </Container>
</section> 